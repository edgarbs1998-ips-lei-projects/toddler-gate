<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.toddlergate12">

    <uses-permission android:name="android.permission.REORDER_TASKS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name="parents_area.parents_area_main"
            android:theme="@style/TitleBarActivity"
            android:label="Parents Area"></activity>
        <activity android:name="bd_related.ActivitySetPassword"
            android:label="@string/app_title"

            android:theme="@style/ActionBarTheme"
            ></activity>
        <activity android:name="bd_related.NewPassword"></activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--<activity android:name=".MainActivity"></activity>-->
    </application>

</manifest>